# FastAPI portfolio backend. Copy this file to .env.local and adjust as needed.
# This value is read by the server-side /api/chat proxy and is not exposed to the browser.
PORTFOLIO_BACKEND_API_URL=http://localhost:8000
